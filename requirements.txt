# OmicsClaw — Bot Runtime Dependencies
# Install: pip install -r requirements.txt

# Discord bot
discord.py>=2.3.0

# SSH remote execution
asyncssh>=2.14.0

# HTTP client (LLM API calls)
aiohttp>=3.9.0

# Config / YAML parsing
pyyaml>=6.0
python-dotenv>=1.0.0

# Credential encryption (SSH password vault)
cryptography>=41.0.0

# SSL certificate bundle (required by discord.py on some systems)
certifi>=2024.0.0

# Dashboard (optional)
fastapi>=0.109.0
uvicorn>=0.27.0
gradio>=4.0.0
websockets>=12.0
psutil>=5.9.0
